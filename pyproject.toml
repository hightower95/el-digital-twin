[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta" 

[project]
name = "el-analysis-toolkit"
version = "0.1.0"
description = "A Python module for demonstration purposes"
authors = [{ name = "Your Name", email = "your.email@example.com" }]
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "loguru>=0.7.0"
]